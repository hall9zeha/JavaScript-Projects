//Pure functions, retorna la misma cantidad de datos que recibe, y nó modifica los datos originales

const num =10;
const duplicate = n => n  *2;

const result= duplicate(num)
console.log(result);
console.log(num);
